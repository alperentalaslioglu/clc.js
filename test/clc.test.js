var clcjs = require('../src/clc.js');
var expect = require('chai').expect;

describe('calcDistBetween function unit tests', function () {

    it('valid argument tests 1 ', function () {
        expect(true).to.be.true;
    });

    it('valid argument tests 2 ', function () {
        expect(true).to.be.true;
    });

    it('valid argument tests 3 ', function () {
        expect(true).to.be.true;
    });
    
});